# Hearthkeeper user blacklist (拉黑用户).
#
# One user id per line (format: u_<discordUserId>). `#` starts a comment.
#
# Manage from Discord: !banuser @member | !unbanuser @member|<id> | !bannedlist
#
# (empty)

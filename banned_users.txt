# Hearthkeeper user blacklist (拉黑用户).
#
# One user id per line (format: u_<discordUserId>). `#` starts a
# comment. A blacklisted member's messages are removed INSTANTLY on
# ingest — whatever the content, no Mind call.
#
# Manage from Discord:
#   !banuser    @user   — blacklist a member (and try to ban them)
#   !unbanuser  @user   — remove from the blacklist
#   !bannedlist         — list blacklisted members
#
# (empty)
u_1243058465652478053

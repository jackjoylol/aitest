# Hearthkeeper whitelist — safe-word dictionary.
#
# One term per line. `#` starts a comment. MATCHING RULES: English/ASCII
# terms match as WHOLE WORDS (word boundaries); Chinese/non-ASCII terms
# match as substrings. Messages containing a whitelisted term are
# allowed INSTANTLY — no Mind call, no waiting.
# The blacklist is checked FIRST and wins over the whitelist.
#
# (dictionary is empty)
